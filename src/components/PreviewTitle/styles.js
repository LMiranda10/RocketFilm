import styled from "styled-components";

export const Container = styled.div`
    margin-top: 25px;
    display: flex;
    align-items: center;
    gap: 20px;

    > div {
        display: flex;
        gap: 10px;

    }
`